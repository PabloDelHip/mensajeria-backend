class UnitController {
  /**
   * Creates an instance of UnitController.
   * @param {Object} UnitsService Units service
   * @memberof UnitController
   */
  constructor(UnitsService) {
    this.UnitsService = UnitsService;
  }
}