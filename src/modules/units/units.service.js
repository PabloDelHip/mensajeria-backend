class UnitsService {
  /**
   * Repositories
   * @Injectable
   * @param {Object} UnitsRepository Units repository
   */
  constructor(UnitsRepository) {
    this.UnitsRepository = UnitsRepository;
  }

}